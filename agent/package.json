{
  "name": "workshop-caption-agent",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Standalone LiveKit Agents worker that subscribes to the facilitator's audio track server-side and publishes live captions, so captions work without the browser mic control. See docs/TRANSLATION_ARCHITECTURE.md Part 2. Deployed separately from the Next.js app — it needs a persistent process, not a serverless function.",
  "scripts": {
    "dev": "tsx src/worker.ts dev",
    "start": "tsx src/worker.ts start"
  },
  "dependencies": {
    "@livekit/agents": "^1.5.5",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1"
  }
}
